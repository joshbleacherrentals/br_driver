# ios/ and android/ are generated by `expo prebuild` and gitignored.
# EAS builds fresh from prebuild, so these dirs don't exist in CI.
# Excluding them keeps local and EAS fingerprints consistent.
ios/**/*
android/**/*
